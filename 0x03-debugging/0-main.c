#include "main.h"
#include <stdio.h>
/**
 *positive_or_negative - Entry point
 *Return: Always 0 (success)
 *@i: return value
 *Description: write a program that checks another program
*/

void positive_or_negative(int i)
{
	if (i == 0)
	{
		printf("%d is zero\n", i);
	}
}
