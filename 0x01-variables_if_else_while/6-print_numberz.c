#include <stdio.h>


/**
 *main - Entry point
 *Return: Always 0 (success)
 *Description: write a C program that prints the alphabet in lowercase
*/

int main(void)

{
	int h;

	for (h = 0; h <= 9; h++)
	{
		putchar(h);
	}
		putchar('\n');
	return (0);
}
