#include <stdlib.h>
#include "main.h"
/**
 *
 *
 */
char *string_nconcat(char *s1, char *s2, unsugned int n)
{
	char *new;
	int l1, l2;;

	if (s1 == NULL)
		s1 = "";
	if (s2 == NULL)
		s2 = "";

	m = n = 0;
	while (s1[m] != '\0')
		l1++;
	while (s2[n] != '\0')
		l2++;
}
